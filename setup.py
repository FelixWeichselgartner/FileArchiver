from setuptools import setup

setup(
   name='FileArchiver',
   version='1.0',
   description='archives files',
   author='FelixWeichselgartner',
   author_email='info@felix-weichselgartner.de',
   packages=['FileArchiver'],
   install_requires=['wheel'],
)
